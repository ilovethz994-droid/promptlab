{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm build:core && pnpm -F @prompt-optimizer/ui build:bundle && pnpm build:web",
  "outputDirectory": "packages/web/dist",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/_vercel/:path*",
      "destination": "/_vercel/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "github": {
    "silent": true
  },
  "git": {
    "deploymentEnabled": {
      "main": true,
      "master": true
    }
  },
  "build": {
    "env": {
      "VITE_VERCEL_DEPLOYMENT": "true"
    }
  }
}
